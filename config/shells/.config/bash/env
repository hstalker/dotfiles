#!/usr/bin/env bash
###############################################################################
# Setup environment for bash
# Run shell/env in order to get appropriate $BASH_ENV
. "${XDG_CONFIG_HOME:-$HOME/.config}/shell/env"

# Load shell specific env modules
load_per_application_modules env bash
