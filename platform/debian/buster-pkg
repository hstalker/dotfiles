#! /usr/bin/env bash

SCRIPT_DIR="$( cd "$( dirname "${BASH_SOURCE[0]}" )" >/dev/null 2>&1 && pwd )"

install_package_sources() {
  if [[ ! -e /etc/apt/sources.list.d/buster-backports.list ]]; then
    echo "Adding buster-backports to sources..."
    sudo mkdir -p /etc/apt/sources.list.d/
    sudo bash -c 'echo "deb http://deb.debian.org/debian buster-backports main" \
      >/etc/apt/sources.list.d/buster-backports.list'
  fi
}

uninstall_package_sources() {
  if [[ -e /etc/apt/sources.list.d/buster-backports.list ]]; then
    echo "Removing buster-backports from sources..."
    sudo rm -f /etc/apt/sources.list.d/buster-backports.list
  fi
}

install_packages() {
  echo "Installing buster specific packages..."
  return
}

uninstall_package_sources
install_package_sources
install_packages

